{
  "name": "open-fit-web",
  "version": "0.0.0",
  "private": true,
  "description": "Open Fit web dashboard — React + Vite + TS, API-first, themable.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "gen:api": "echo 'TODO: generate the typed API client from the ofit-api OpenAPI schema at $VITE_API_BASE/api-docs/openapi.json (e.g. openapi-typescript or @hey-api/openapi-ts) into src/api/generated/. Do NOT hand-write API types — see src/api/README.md.' && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
